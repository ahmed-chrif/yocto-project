# 08 — What to Learn Next

## Level 1
Fix known issues; real layer READMEs; portable local.yml; remove debug-tweaks.

## Level 2
Hello recipe in meta-chrif; a bbappend; IMAGE_INSTALL / IMAGE_FEATURES.

## Level 3
Out-of-tree module (module.bbclass) + DT overlay — the missing BSP story.

## Level 4
Read BACKPORT-CHANGES.md; own systemd unit writing under /var; try zram.

## Level 5
Kas-only truth; never commit build/layers; optional CI.

## Checklist
Distro vs machine vs image; why pin Kas commits; how bbappend matches; how WIC lays out disk; why empty rootfs /var; what LAYERSERIES_COMPAT does.
