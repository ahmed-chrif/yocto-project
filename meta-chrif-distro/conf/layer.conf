# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-chrif-distro"
BBFILE_PATTERN_meta-chrif-distro = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-chrif-distro = "6"

LAYERDEPENDS_meta-chrif-distro = "core"
LAYERSERIES_COMPAT_meta-chrif-distro = "scarthgap"
